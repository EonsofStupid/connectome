# connectome — RETIRED TREE (wrong split)

This checkout is **not** the engine.

It is the later two-repo mistake: Surreal vectors excised so a sibling
"TotalRecall" product could own recall. That split is **void**. There is no
second engine, no treasure repo, and no map/treasure architecture.

**One engine. One tool.** Graph, documents, and relations from Surreal.
Surreal's native vector path replaced in-process by Qdrant `segment` tech
(`DEFINE INDEX … QORTEX`). Stdio / signals on the face.

The live fused tree is [`EonsofStupid/surrealdb`](https://github.com/EonsofStupid/surrealdb)
@ `03e44e2`, consumed by `clyffy-connectome`. Do not resume Phase 2 excision.
Do not recreate TotalRecall.
