# RETIRED — wrong split

This file sold a two-engine plan: excise Surreal vectors, then plug in a sibling
TotalRecall repo. That plan is **void**.

Connectome is **one engine and one tool**. Surreal keeps graph, documents, and
relations. Surreal's native vector path is replaced in-process by Qdrant
`segment` tech as `DEFINE INDEX … QORTEX`. The face is stdio / signals.

The live fused tree is [`EonsofStupid/surrealdb`](https://github.com/EonsofStupid/surrealdb)
@ `03e44e2`. Do not resume excision. Do not recreate TotalRecall.
