#!/usr/bin/env bash
# RETIRED. This script belonged to the two-repo split (vector-index OFF so a
# sibling TotalRecall product could own recall). That split is void.
#
# One engine: fused QORTEX tree at EonsofStupid/surrealdb @ 03e44e2.
echo "this build script is retired; the two-repo split is void" >&2
echo "use the fused QORTEX engine (EonsofStupid/surrealdb @ 03e44e2)" >&2
exit 2
